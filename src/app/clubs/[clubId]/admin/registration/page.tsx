"use client";

export default function AdminEditPage({
  params,
}: {
  params: { clubId: string };
}) {
  console.log(params);

  return (
    <div className="h-[calc(100vh-4rem)] w-full">
      <h1>heyyy</h1>
    </div>
  );
}
